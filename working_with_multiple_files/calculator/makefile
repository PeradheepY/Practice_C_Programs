# Compiler
CC = gcc

# All source files
SRCS = main.c calculator.c calculator.h

# Output executable
TARGET = calci

# Default rule to build the executable
all:
	$(CC) $(SRCS) -o $(TARGET)

# Clean up generated files
clean:
	rm -f $(TARGET)

